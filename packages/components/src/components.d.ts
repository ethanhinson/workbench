/* eslint-disable */
/* tslint:disable */
/**
 * This is an autogenerated file created by the Stencil compiler.
 * It contains typing information for all components that exist in this project.
 */
import { HTMLStencilElement, JSXBase } from "@stencil/core/internal";
import { Annotation, ChatMessage } from "./models";
export { Annotation, ChatMessage } from "./models";
export namespace Components {
    /**
     * wb-annotation — a single annotation attached to an element of the prototype.
     * Presentational + one interaction: the human edits the note and confirms.
     * State it owns is only the in-progress edit; the committed annotation is
     * emitted upward via `annotationCommit`. It never talks to a harness — the
     * shell/adapter collects these into an envelope.
     */
    interface WbAnnotation {
        /**
          * Stable id for this annotation.
         */
        "annotationId": string;
        /**
          * Bounding box "x,y,w,h" of the target at capture time.
          * @default ""
         */
        "box": string;
        /**
          * The committed note text (empty until the human writes one).
          * @default ""
         */
        "note": string;
        /**
          * CSS selector of the annotated element.
         */
        "selector": string;
    }
    /**
     * wb-chat — the chat experience for the prototyper's side panel.
     * The human talks to the agent here; the adapter streams agent turns back in.
     * The component owns the transcript and the input draft, and emits outgoing
     * user messages via `chatSend`. Agent text arrives through the imperative
     * `appendAgentChunk`/`endAgentTurn` methods so the adapter can stream tokens
     * without re-passing the whole array each frame.
     */
    interface WbChat {
        /**
          * Append (or extend) the current streaming agent turn.
         */
        "appendAgentChunk": (chunk: string) => Promise<void>;
        /**
          * Disable the composer (e.g. while the agent is mid-turn).
          * @default false
         */
        "busy": boolean;
        /**
          * Mark the current streaming agent turn complete.
         */
        "endAgentTurn": () => Promise<void>;
        /**
          * Seed transcript. After mount, mutate via methods/events.
          * @default []
         */
        "messages": ChatMessage[];
        /**
          * Placeholder for the composer.
          * @default "Message the agent…"
         */
        "placeholder": string;
    }
}
export interface WbAnnotationCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLWbAnnotationElement;
}
export interface WbChatCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLWbChatElement;
}
declare global {
    interface HTMLWbAnnotationElementEventMap {
        "annotationCommit": Annotation;
        "annotationRemove": { id: string };
    }
    /**
     * wb-annotation — a single annotation attached to an element of the prototype.
     * Presentational + one interaction: the human edits the note and confirms.
     * State it owns is only the in-progress edit; the committed annotation is
     * emitted upward via `annotationCommit`. It never talks to a harness — the
     * shell/adapter collects these into an envelope.
     */
    interface HTMLWbAnnotationElement extends Components.WbAnnotation, HTMLStencilElement {
        addEventListener<K extends keyof HTMLWbAnnotationElementEventMap>(type: K, listener: (this: HTMLWbAnnotationElement, ev: WbAnnotationCustomEvent<HTMLWbAnnotationElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLWbAnnotationElementEventMap>(type: K, listener: (this: HTMLWbAnnotationElement, ev: WbAnnotationCustomEvent<HTMLWbAnnotationElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLWbAnnotationElement: {
        prototype: HTMLWbAnnotationElement;
        new (): HTMLWbAnnotationElement;
    };
    interface HTMLWbChatElementEventMap {
        "chatSend": { text: string };
    }
    /**
     * wb-chat — the chat experience for the prototyper's side panel.
     * The human talks to the agent here; the adapter streams agent turns back in.
     * The component owns the transcript and the input draft, and emits outgoing
     * user messages via `chatSend`. Agent text arrives through the imperative
     * `appendAgentChunk`/`endAgentTurn` methods so the adapter can stream tokens
     * without re-passing the whole array each frame.
     */
    interface HTMLWbChatElement extends Components.WbChat, HTMLStencilElement {
        addEventListener<K extends keyof HTMLWbChatElementEventMap>(type: K, listener: (this: HTMLWbChatElement, ev: WbChatCustomEvent<HTMLWbChatElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLWbChatElementEventMap>(type: K, listener: (this: HTMLWbChatElement, ev: WbChatCustomEvent<HTMLWbChatElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLWbChatElement: {
        prototype: HTMLWbChatElement;
        new (): HTMLWbChatElement;
    };
    interface HTMLElementTagNameMap {
        "wb-annotation": HTMLWbAnnotationElement;
        "wb-chat": HTMLWbChatElement;
    }
}
declare namespace LocalJSX {
    type OneOf<K extends string, PropT, AttrT = PropT> = { [P in K]: PropT } & { [P in `attr:${K}`]?: never } | { [P in `attr:${K}`]: AttrT } & { [P in K]?: never };

    /**
     * wb-annotation — a single annotation attached to an element of the prototype.
     * Presentational + one interaction: the human edits the note and confirms.
     * State it owns is only the in-progress edit; the committed annotation is
     * emitted upward via `annotationCommit`. It never talks to a harness — the
     * shell/adapter collects these into an envelope.
     */
    interface WbAnnotation {
        /**
          * Stable id for this annotation.
         */
        "annotationId": string;
        /**
          * Bounding box "x,y,w,h" of the target at capture time.
          * @default ""
         */
        "box"?: string;
        /**
          * The committed note text (empty until the human writes one).
          * @default ""
         */
        "note"?: string;
        /**
          * Fired when the human commits (or updates) the note.
         */
        "onAnnotationCommit"?: (event: WbAnnotationCustomEvent<Annotation>) => void;
        /**
          * Fired when the human removes this annotation.
         */
        "onAnnotationRemove"?: (event: WbAnnotationCustomEvent<{ id: string }>) => void;
        /**
          * CSS selector of the annotated element.
         */
        "selector": string;
    }
    /**
     * wb-chat — the chat experience for the prototyper's side panel.
     * The human talks to the agent here; the adapter streams agent turns back in.
     * The component owns the transcript and the input draft, and emits outgoing
     * user messages via `chatSend`. Agent text arrives through the imperative
     * `appendAgentChunk`/`endAgentTurn` methods so the adapter can stream tokens
     * without re-passing the whole array each frame.
     */
    interface WbChat {
        /**
          * Disable the composer (e.g. while the agent is mid-turn).
          * @default false
         */
        "busy"?: boolean;
        /**
          * Seed transcript. After mount, mutate via methods/events.
          * @default []
         */
        "messages"?: ChatMessage[];
        /**
          * Fired when the human sends a message.
         */
        "onChatSend"?: (event: WbChatCustomEvent<{ text: string }>) => void;
        /**
          * Placeholder for the composer.
          * @default "Message the agent…"
         */
        "placeholder"?: string;
    }

    interface WbAnnotationAttributes {
        "annotationId": string;
        "selector": string;
        "box": string;
        "note": string;
    }
    interface WbChatAttributes {
        "busy": boolean;
        "placeholder": string;
    }

    interface IntrinsicElements {
        "wb-annotation": Omit<WbAnnotation, keyof WbAnnotationAttributes> & { [K in keyof WbAnnotation & keyof WbAnnotationAttributes]?: WbAnnotation[K] } & { [K in keyof WbAnnotation & keyof WbAnnotationAttributes as `attr:${K}`]?: WbAnnotationAttributes[K] } & { [K in keyof WbAnnotation & keyof WbAnnotationAttributes as `prop:${K}`]?: WbAnnotation[K] } & OneOf<"annotationId", WbAnnotation["annotationId"], WbAnnotationAttributes["annotationId"]> & OneOf<"selector", WbAnnotation["selector"], WbAnnotationAttributes["selector"]>;
        "wb-chat": Omit<WbChat, keyof WbChatAttributes> & { [K in keyof WbChat & keyof WbChatAttributes]?: WbChat[K] } & { [K in keyof WbChat & keyof WbChatAttributes as `attr:${K}`]?: WbChatAttributes[K] } & { [K in keyof WbChat & keyof WbChatAttributes as `prop:${K}`]?: WbChat[K] };
    }
}
export { LocalJSX as JSX };
declare module "@stencil/core" {
    export namespace JSX {
        interface IntrinsicElements {
            /**
             * wb-annotation — a single annotation attached to an element of the prototype.
             * Presentational + one interaction: the human edits the note and confirms.
             * State it owns is only the in-progress edit; the committed annotation is
             * emitted upward via `annotationCommit`. It never talks to a harness — the
             * shell/adapter collects these into an envelope.
             */
            "wb-annotation": LocalJSX.IntrinsicElements["wb-annotation"] & JSXBase.HTMLAttributes<HTMLWbAnnotationElement>;
            /**
             * wb-chat — the chat experience for the prototyper's side panel.
             * The human talks to the agent here; the adapter streams agent turns back in.
             * The component owns the transcript and the input draft, and emits outgoing
             * user messages via `chatSend`. Agent text arrives through the imperative
             * `appendAgentChunk`/`endAgentTurn` methods so the adapter can stream tokens
             * without re-passing the whole array each frame.
             */
            "wb-chat": LocalJSX.IntrinsicElements["wb-chat"] & JSXBase.HTMLAttributes<HTMLWbChatElement>;
        }
    }
}
