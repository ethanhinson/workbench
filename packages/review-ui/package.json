{
  "name": "@workbench/review-ui",
  "version": "0.0.0",
  "description": "The browser shell: 80% resizable prototype pane + 20% chat pane. Hosts the agent-authored prototype and the review-chrome, and streams the harness chat.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workbench/envelope": "workspace:*",
    "@workbench/review-chrome": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
