{
  "name": "@workbench/review-bridge",
  "version": "0.0.0",
  "private": true,
  "description": "Local bridge: owns the harness session, serves the prototype to the browser, and pushes structured review feedback back into the running agent turn. Harness-neutral core + thin per-harness adapters.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch dist/index.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workbench/envelope": "workspace:*",
    "@workbench/review-chrome": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
